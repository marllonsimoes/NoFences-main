<Controls:Flyout x:Class="NoFences.View.MonitoredPathFlyout"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:NoFences.View"
            xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
            xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
            mc:Ignorable="d" 
            d:DesignHeight="519" d:DesignWidth="800"
            CloseButtonIsCancel="True"
            Header="Monitored path"
            Position="Right"
            Theme="Adapt" Loaded="Flyout_Loaded">

    <StackPanel Margin="10" Orientation="Vertical" Width="800">
        <Label Content="ID:" Grid.Row="0" Grid.Column="0"/>
        <Label Content="{Binding SelectedMonitoredPath.Id}" Grid.Row="0" Grid.Column="0"/>
        <Grid>
            <Grid.Resources >
                <Style TargetType="{x:Type Label}">
                    <Setter Property="Margin" Value="3"/>
                </Style>
                <Style TargetType="{x:Type TextBox}">
                    <Setter Property="Margin" Value="3"/>
                </Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Margin" Value="3"/>
                </Style>
                <Style TargetType="{x:Type ListBox}">
                    <Setter Property="Margin" Value="3"/>
                </Style>
                <Style TargetType="{x:Type Controls:ToggleSwitch}">
                    <Setter Property="Margin" Value="3"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="400"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <Label Content="Path:" Grid.Row="0" Grid.Column="0"/>
            <TextBox x:Name="SelectedPath" Grid.Row="0" Grid.Column="1" Text="{Binding SelectedMonitoredPath.Path}" Height="30"/>
            <Button Content="Select..." Grid.Row="0" Grid.Column="2" Margin="3" Width="90" Height="30" Click="SelectPath_Click"/>

            <Label Content="Exclude:" Grid.Row="1" Grid.Column="0"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedMonitoredPath.Exclude}" Height="30"/>

            <Label Content="Recursive:" Grid.Row="2" Grid.Column="0"/>
            <Controls:ToggleSwitch Grid.Row="2" Grid.Column="1" IsOn="{Binding SelectedMonitoredPath.Recursive}" Height="30"/>

            <Label Content="Configuration:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Top"/>
            <ListBox Grid.RowSpan="2" Grid.Row="3" Grid.Column="1" ItemsSource="{Binding FolderConfigurationList}" SelectionMode="Single" SelectedItem="{Binding SelectedFolderConfiguration}" Height="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="200">
                            <Grid.Resources >
                                <Style TargetType="{x:Type Label}">
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </Grid.Resources>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="70" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="24"/>
                                <RowDefinition Height="25"/>
                                <RowDefinition Height="25"/>
                                <RowDefinition Height="25"/>
                                <RowDefinition Height="25"/>
                                <RowDefinition Height="25"/>
                            </Grid.RowDefinitions>
                            <Label Content="Name:" Grid.Row="0" Grid.Column="0"/>
                            <Label Content="{Binding Name}" Grid.Row="0" Grid.Column="1"/>
                            
                            <Label Content="Desc:" Grid.Row="1" Grid.Column="0"/>
                            <Label Content="{Binding Description}" Grid.Row="1" Grid.Column="1"/>

                            <Label Content="File filter:" Grid.Row="2" Grid.Column="0"/>
                            <Label Content="{Binding FileFilter}" Grid.Row="2" Grid.Column="1"/>

                            <Label Content="Folder in file name:" Grid.Row="3" Grid.Column="0"/>
                            <Label Content="{Binding FolderInFileName}" Grid.Row="3" Grid.Column="1"/>

                            <Label Content="Folder processor:" Grid.Row="4" Grid.Column="0"/>
                            <Label Content="{Binding FolderNameProcessor}" Grid.Row="4" Grid.Column="1"/>

                            <Label Content="File processor:" Grid.Row="5" Grid.Column="0"/>
                            <Label Content="{Binding FileNameProcessor}" Grid.Row="5" Grid.Column="1"/>

                            <Label Content="Movo to:" Grid.Row="6" Grid.Column="0"/>
                            <Label Content="{Binding MoveTo.Path}" Grid.Row="6" Grid.Column="1"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Add..." Grid.Row="3" Grid.Column="2" Margin="3" Width="90" Height="30" VerticalAlignment="Top" Click="FolderConfigurationClick" />
            <Button Content="Edit" IsEnabled="{Binding TargetNullValue=SelectedFolderConfiguration}" Grid.Row="4" Grid.Column="2" Margin="3" Width="90" Height="30" VerticalAlignment="Top" Click="EditFolderConfigurationClick" />
            
            <Button Content="Save" Grid.Row="5" Grid.Column="2" Margin="3" Width="90" Height="30" VerticalAlignment="Top" Click="SaveMonitoredPath" />
        </Grid>
    </StackPanel>
</Controls:Flyout>